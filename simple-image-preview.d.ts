/**
 * A lightweight image preview library
 * @param s default: document.querySelectorAll('img')
 */
export default function simpleImagePreview(s?: string | NodeList, options?: {
    zIndex: string;
}): void;
